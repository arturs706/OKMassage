width: 3rem;
        height: 0.3rem; 
        background-color: ${({ open }) => open ? '#000' : '#000'};
        border-radius: 20rem;
        transform-origin: -0.63vh;
        transition: all 0.3s linear;
